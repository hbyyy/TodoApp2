server {
    listen 80;

    server_name 13.125.186.75;

    charset utf-8;

    location /static/ {
            root /srv/Todoapp/app;
        }

    location / {
        proxy_pass      http://unix:/run/todos.sock;
    }
}

